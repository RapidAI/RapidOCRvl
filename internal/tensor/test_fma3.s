//go:build amd64
#include "textflag.h"

TEXT ·testFMA3(SB), NOSPLIT, $0-28
	MOVQ a_base+0(FP), SI
	VMOVUPS (SI), Y0
	VMOVUPS 32(SI), Y1
	VMOVUPS 64(SI), Y2
	VFMADD132PS Y2, Y1, Y0
	VEXTRACTF128 $1, Y0, X1
	VADDPS X0, X1, X1
	VHADDPS X1, X1, X1
	VHADDPS X1, X1, X1
	VZEROUPPER
	MOVSS X1, ret+24(FP)
	RET
